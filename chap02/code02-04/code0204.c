#include <stdio.h>

int main(void)
{
  int number = 7;        /* 変数numberを7で初期化 */
  printf("私の好きな数字は%d\n", number);
  number = 16;           /* 16を代入 */
  printf("いや、本当に好きなのは%d\n", number);
  return 0;
}
